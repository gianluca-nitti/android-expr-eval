<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:tools="http://schemas.android.com/tools">

    <item android:id="@+id/action_context"
          android:title="@string/context"
          tools:showAsAction="ifRoom"/>

    <item android:id="@+id/action_clearctx"
          android:title="@string/clearContext"
          tools:showAsAction="never"/>

    <item android:id="@+id/action_clearout"
          android:title="@string/clearOutput"
          tools:showAsAction="never"/>

    <item android:id="@+id/action_verbose"
          android:title="@string/showSteps"
          android:checkable="true"
          android:checked="true"
          tools:showAsAction="never"/>

    <item android:id="@+id/action_input"
          android:title="@string/echoInput"
          android:checkable="true"
          android:checked="true"
          tools:showAsAction="never"/>

    <item android:id="@+id/action_help"
          android:title="@string/help"
          tools:showAsAction="never"/>

</menu>