<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:tools="http://schemas.android.com/tools">

    <item android:id="@+id/action_context"
          android:title="Context"
          tools:showAsAction="ifRoom"/>

    <item android:id="@+id/action_clearctx"
          android:title="Clear context"
          tools:showAsAction="never"/>

    <item android:id="@+id/action_clearout"
          android:title="Clear output"
          tools:showAsAction="never"/>

    <item android:id="@+id/action_help"
          android:title="Help"
          tools:showAsAction="never"/>

</menu>